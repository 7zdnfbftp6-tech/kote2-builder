# 対象リスト20社分のページを一括生成

- **保存理由**: 「とりあえずページを量産してください」の依頼に応じて、承認済みテンプレートから20社分を構造だけ一括生成した区切り
- **完了したこと**:
  - `content/target-companies.md` の対象20社(国際興業を除く)について `content/companies/{slug}.json` と `output/pages/{slug}.html` を生成
  - 給与・勤務地・電話番号・写真URL・企業情報など、会社ごとに異なる事実情報は実データが無いため、実在しない情報を作らず【要確認】のプレースホルダーで統一(ページタイトルにも「【求人ページ・下書き】」と明記)
  - `content/target-companies.md` のチェックリストを更新し、下書きから実データへの仕上げ方の手順を追記
  - `dashboard.html` に「下書きページを実データで仕上げる」の提案と、今回の一括生成を activity に追記
- **決定事項**: バッジ・訴求文などの汎用的な宣伝文は元テンプレートの雰囲気を踏襲して共通の下書きとして流用。ただし本社所在地・事業内容・運行エリア・給与・電話番号など、会社ごとの事実に関わる項目は実データ不明のため断定せず、プレースホルダーのまま生成(実在する会社について誤った情報を作らないため)
- **未完了タスク**: 20社分すべて、給与・勤務地・条件バッジ・写真URL・企業情報などの実データ確認が未着手
- **次にやること**: 会社ごとに「〇〇の求人ページを実データで仕上げて」と話しかけてもらいながら、`content/companies/{slug}.json` を実データへ更新 → `output/pages/{slug}.html` を再生成
- **再開・検証コマンド**: なし(ビルドツール不要。生成物は `output/pages/*.html` をブラウザで直接開いて確認)
- **検証結果**: 全21ページ(国際興業含む)で `{{`残存なし・`REPEAT`残存なしを grep で確認。1社(東武鉄道)を抜き取りでスクリーンショット確認し、レイアウト崩れなし
