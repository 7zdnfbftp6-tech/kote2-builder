# 「求人」カスタム投稿タイプ向けの一括下書き作成を追加

- **保存理由**: ユーザーの実サイトを確認したところ、「求人」は独自のカスタム投稿タイプ(post_type=job)で、ACFではなく `jd-wrap` 構造のHTMLを本文に直接入れる方式だと判明。既存の国際興業バスの投稿がまさにその形だった
- **完了したこと**:
  - `output/pages/{slug}.html` から `<div class="jd-wrap">...</div>` 部分だけを抽出し、48社ぶん(国際興業バスは既に公開済みのため除外)「求人」投稿として下書き作成する `wordpress/bulk-import-job-cpt.codesnippets.php` を作成
  - `wordpress/README.md` を「A. 求人への一括下書き作成」「B. 会社紹介記事(ACF)への一括下書き作成」の2系統に整理
- **決定事項**: 「エリア」タクソノミー(関東/埼玉県/東京都等)は自動設定せず空欄のまま(ユーザーへの確認質問に対する回答が無かったため、安全側=手動設定に倒した)。国際興業バスは実サイトに既存の公開投稿があるため、重複作成を避けるためスクリプトの対象から除外
- **未完了タスク**: ユーザー側でのスクリプト実行(まだ本番サイトでは未実行)。実行後、給与・勤務地・写真・応募URル等の【要確認】項目とエリアタクソノミーを埋めて公開する作業
- **次にやること**: ユーザーがスクリプトを実行し、結果を報告してもらう
- **再開・検証コマンド**: `php -l` で構文チェック済み(先頭に一時的に `<?php` を付けて検証。配布物自体はCode Snippets用に `<?php` 無しの形を保持)
- **検証結果**: PHP構文チェックOK。生成された48件のフラグメントに `{{` 等の残存プレースホルダーなし
