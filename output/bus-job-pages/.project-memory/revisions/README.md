# リビジョン

作業の節目や中断・AI切り替えの前に、現在地・決定事項・未完了タスク・次にやることをスナップショットとして残す場所です。

「セーブして」と依頼すると、日時ときっかけを付けた Markdown ファイルをこのディレクトリへ保存します。過去の記録は自動削除せず、必要に応じて `archive/` へ要約します。

実際のスナップショットは `.project-memory/.gitignore` により既定では Git 管理されません。秘密情報は書かないでください。
