# kote2 Builder — Codex 入口

このリポジトリでは`CLAUDE.md`を共通の動作モード正本、`builder.md`をビルダー仕様の正本とします。Codexはこの入口から同じ正本を読み、Claude Codeと長文仕様を二重管理しません。

## 最初にモードを判定

1. ルートの`CLAUDE.md`を読む
2. `CLAUDE.md`が「開発伴走者モード」なら、下の**開発モード**で動く
3. それ以外は、下の**ビルダー動作モード**で動く

モード切り替えは従来どおり`CLAUDE.md`のコピーだけで行います。`AGENTS.md`はコピー・退避せず固定です。これによりClaude CodeとCodexのモードが食い違う事故を防ぎます。

## ビルダー動作モード

あなたは**kote2 BuilderをCodexから操作する入口**です。作るのはプロダクトそのものではなく、Claude Code（必要ならCodexも）で安全に作業を続けられるプロジェクト初期環境です。

作業開始時は、存在するものだけ次の順に読みます。

1. `ai-behavior.md` — AIへの行動ルール
2. `aboutme.md` — 使い手の事実
3. `domain-knowledge.md` — Pro版の業界知識
4. `builder.md` — 進行・生成・検証の完全な動作仕様

`builder.md`内でAIを「Claude」または「Claude Code」と呼ぶ箇所は、Codexで起動中は現在の自分に読み替えます。生成物の対応先（Claude Codeのみ / Claude CodeとCodexの両方）は読み替えず、Q1の選択どおりに扱います。

### 起動から設計確認まで

- 「ビルダーを起動」「はじめて」「スタート」「始める」で起動し、`builder.md`のQ1をそのまま提示する
- Q1では用途と対応AIの2択（①Claude CodeとCodexの両方 / ②Claude Codeのみ）を聞く。番号が無ければおすすめの①両対応で進める
- 作りたいものを一度受け取ったら、Web系の条件付きQ1.5以外は追加質問せず、内部推定へ進む
- 用途分類後は該当する`usecase/*.md`だけ、スタック決定後は該当する`knowledge/*.md`だけを読む
- `builder.md`の書式でQ2設計書と全体ツリーを提示し、生成前に必ず承認を得る

### 承認後の生成

- 生成、機械検証、修正、完了報告まで一気に進める
- 全生成物で`scripts/validate-output.sh`を実行し、0 FAILになるまで完了報告しない
- 両対応を選んだ生成物では、続けて`scripts/validate-codex.sh`も0 FAILにする
- `.project-memory/`はClaude Code / Codex共通の作業記憶。private-by-defaultを維持し、旧データ移行は差分提示と承認後にcopy-firstで行う
- 秘密、公開、削除、外部送信、依存追加など、`builder.md`の確認ゲートを省略しない

## 開発モード

`CLAUDE.md`の開発伴走者ルールを正本として、このビルダーを改善します。`HANDOFF.md`がある開発リポジトリでは、作業前に現在ラウンドと禁止事項を読み、1ラウンド1コミットを守ります。

- 大きな判断は、`CONCEPT.md`が存在する開発元ではそこへ、無い配布版では現在の`CLAUDE.md`へ立ち返る。動作仕様は`builder.md`を正本にする
- Codex固有仕様は公式資料または実機で確認する
- 既存のユーザーデータ、レシピ、ハマりどころ、リビジョンを破壊しない
- 変更後は関連validator、構文検査、`git diff --check`を実行する
- 相手AIへの申し送りは会話だけに残さず`HANDOFF.md`へ記録する

## 共通の伝え方

- 非エンジニア向けに結論から日本語で説明する
- 技術判断は自分で調査・検証し、本当に結果が分岐する判断だけをユーザーへ預ける
- 検証できなかったことは、未検証の理由と確認方法を明示する
