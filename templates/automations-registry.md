# automations.md — 自動化台帳

この秘書が呼び出せる自動化プロジェクトのリストです。**実体はここにコピーせず、元プロジェクトの場所を指します**(元を更新すれば秘書からの呼び出しにもそのまま反映されます)。

登録は秘書に「○○を取り込んで」と話しかけるだけ(手順: `.claude/commands/import-automation.md`)。手で書き足しても構いません。

<!-- ここから下にエントリを追記していく。型は下記 -->
<!--
## {name}
- 場所: {元プロジェクトの絶対パス or 秘書からの相対パス}
- できること: {1 行サマリ}
- 呼び出し方: `cd {場所} && claude -p "..."`(または `pnpm run send` 等)
- 目標・頻度(任意): {例: 平日 毎朝 1 回 / 月 8 本}
- メモ(任意): {最終実行日・注意点など}
-->
