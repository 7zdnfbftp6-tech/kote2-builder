# MIGRATION.md — 旧ビルダー → 新ビルダー本体への引っ越し

新しいバージョンのビルダー(解凍したばかりの本体)に、**前のバージョンで貯めたデータと自作カスタマイズを引き継ぐ**ための手順。

> **「アップデート」との違い**(混同注意):
> - **アップデート**(`builder.md`「既存プロジェクトのアップデート」)= ビルダー → `output/` の**生成プロジェクト(子)**を最新版に揃える。逆向き。
> - **移行(このファイル)** = **旧ビルダー本体 → 新ビルダー本体**(親自身の引っ越し)。運ぶのはユーザー側のデータと自作物。

## 起動トリガー

**基本形**は「古いフォルダのパスを先に貼ってから頼む」:

```
(古いフォルダのパス。例: /Users/you/projects/claude-code-init-builder_v3)
旧ビルダーの内容をこの本体に移行して
```

- 同義の言い方も受け付ける:「このビルダーを本体に移行して」「旧ビルダーから移行して」「(旧バージョンから)引っ越して」
- パスが貼られていなければ、最初に1回だけ聞く(後述「大前提」)。

## 大前提(安全性)

- **このファイルを実行するのは「新しいビルダー」フォルダの中**。
- **コピー元(旧フォルダ)は読むだけ・一切変更しない**(非破壊)。だから失敗しても壊れない・何度でもやり直せる。
- 最初に**旧ビルダーフォルダのパスをユーザーに聞く**(絶対パス。例: `../claude-code-init-builder_v2`)。

---

## フェーズ1:まず「移行設計書」を見せて承認を取る(いきなりコピーしない)

ビルダーの「いきなり生成しない・設計書で OK を取る」と同じ作法。移行も**何を移すかを設計書として提示 → OK → 実行**の順で進める。

### ステップ1-A:比較・検出して 📋移行設計書 を出す(この段階では何もコピー/変更しない)

旧フォルダと新本体を突き合わせ、**持ち物(①)とオーナーの自作物(②③)を1つの `📋 移行設計書` にまとめて**提示し、**ひとつの承認**を取る。承認まで一切コピー・変更しない。承認は「全部おすすめ通りでOK」でも「この行だけこうして」と部分調整でもよい。

#### パートA:持ち物(データ)

> **❌ 最重要・繰り返し起きる事故:持ち物を「同一/差分なし/白紙だから」と移行対象から外さない。**
> 持ち物の5項目は**規定の移行対象**。設計書には必ず**全部 ✅移行対象として載せ**、実行段階で**実際にコピーする(同一でも運ぶ)**。「新旧で中身が同じ → コピー不要」と判断してリストから落とす・スキップ扱いにするのは**禁止**。
> 理由:移行で欲しいのは"確実に運んだ"という**安心**であって、賢い省略ではない。同一でも「✅運びました」と報告されたいのに、「差分なし→不要」と外されると**ユーザーは『自分のファイルが捨てられた?』と毎回不安になる**(この誤りが繰り返し発生した)。**最適化より「規定どおり全部運んで・運んだと報告」を優先**。
> 同一であることを伝えたい時は、対象から外さず、行に「(今回は中身が同じ)」と**注記するだけ**にとどめる。コピーのコストはゼロに近い。

- **持ち物(下の表)**: 存在するものは**全部 ✅移行対象**として載せる(同一でも外さない。上の❌参照)。`output/` だけ本人実行、他は AI が運ぶ。
- **`output/` は中身を一覧化**し、各プロジェクトごとに ✅移行(ソース・設定・`.git`)/ ⏭️スキップ(再生成可能フォルダ)を**ディレクトリツリー**で見せる。容量も添えると判断しやすい。
- **個人データの在りか(取りこぼし不安を先に消す)**: 配布版ではビルダー直下の `aboutme.md` 等は**白紙テンプレ**で、実データは**各 output プロジェクト側**にある。「直下が空でも、個人データは output と一緒に運ばれる」と明記する。

| 運ぶもの(持ち物) | 誰が運ぶか / 中身 |
|---|---|
| `aboutme.md` / `ai-behavior.md` | **AI が自動コピー**。使い手の事実・AI への指示 |
| `domain-knowledge.md`(Pro)/ `design-knowledge.md`(Web・あれば) | **AI が自動コピー**。業界知識・審美眼 |
| `revisions.md` + `revisions/`(あれば) | **AI が自動コピー**。改修ログ(無ければ無視) |
| `recipes/` | **AI が自動コピー**。自作レシピ |
| `output/`(再生成物を除く) | **ユーザー自身が運ぶ**(下記)。生成プロジェクト一式。ソース・設定・`.project-memory/`(旧版は`.claude/.tmp/revisions`も含む)・「ハマりどころ」・各プロジェクトの `aboutme.md` 等の実データ・`.git` |

**`output/` は AI が自動コピーしない(本人の手で運ぶ)**: output は容量が大きく、**ユーザーの本物の作品**なので、AI が勝手に動かさない(事故ったら痛い)。**非エンジニア向けに、まずは"手動コピー"を平易に案内する**(コマンドは前面に出さない):

> 古いビルダーの中の **`output` フォルダ**を、**Finder(Mac)/ エクスプローラー(Windows)で、この新しいビルダーの中にドラッグしてコピー**してください。これであなたの作品が全部移ります。古いフォルダはそのまま残ります。

AI が自動で運ぶのは上の軽いデータだけ。output は本人がドラッグでコピー。

### 生成プロジェクト内の共有作業記憶(v3.20以降)

新しく生成するプロジェクトは、リビジョンやスキル提案の正本を`.project-memory/`に置きます。Claude CodeとCodexのどちらからでも同じ作業史を読め、実データは既定ではGitに載りません。

旧プロジェクトの`.claude/.tmp/revisions/`は、このビルダー本体の引っ越しでは自動移動・削除しません。`output/`と一緒にそのまま保全した後、対象プロジェクトごとに「アップデートして」と依頼してください。差分確認と承認の後にcopy-firstで新側へ移し、件数・内容を検証しても旧側は自動削除しません。新旧が両方ある場合も、確認なしに片方を正本扱いして上書きしないでください。

**コマンドが欲しいと言われた時だけ**、除外つきコピーのコマンドを渡す(自分から押し付けない)。**ユーザーの OS に合わせる**:

- **Mac / Linux**(rsync):

```bash
OLD=/path/to/old-builder   # ← 旧フォルダの絶対パス
rsync -a \
  --exclude=node_modules --exclude=.venv --exclude=venv --exclude=.next \
  --exclude=dist --exclude=build --exclude=.astro --exclude=__pycache__ \
  "$OLD"/output/ ./output/
```

- **Windows**(robocopy。`rsync` は標準では無い。PowerShell / コマンドプロンプトで):

```bat
robocopy "C:\path\to\old-builder\output" ".\output" /E ^
  /XD node_modules .venv venv .next dist build .astro __pycache__
```

`node_modules` などの**再生成可能フォルダ**(`node_modules` / `.venv` / `venv` / `.next` / `dist` / `build` / `.astro` / `__pycache__`)は無くても、各プロジェクトで `pnpm install` 等で復元できる。**手動ドラッグで丸ごとコピーした場合は気にしなくてよい**(容量が増えるだけで動く)。容量を絞りたい人だけ上のコマンドを使う(実測例 4.3GB → 約116MB)。`.git` は残す。

#### パートB:オーナーが作った/書き換えた自作物(②③)

**件数だけでなく、ファイルのリストごと設計書に載せて承認に乗せる**(オーナーが「何が移って・何が眠るか」を一望して確定できるように)。

検出スコープ: `builder.md` / `knowledge/` / `templates/` / `usecase/` / `scripts/` / `recipes/` / `README.md` / `CLAUDE.*.md`。除外: `.git` / `.DS_Store` / `dist/` / `.recipe-test/` とパートA で運ぶ①のパス。

- **②新規**: 旧にあって新に**無い**ファイル(例: 足した `knowledge/laravel-inertia.md`、`templates/rules-laravel.md`)
- **③手編集**: 新にも在るが**中身が違う**ファイル(例: 配線を書き足した `builder.md`、追記した `knowledge/n8n.md`)

**考え方(当たりをつけて承認で確定)**: 「中身が違う=オーナーが編集した(③)」「本体に無い=オーナーが作った可能性が高い(②)」と当たりをつけて、両方を移行対象の**候補**として挙げる。②は稀に「新版が廃止・改名したファイル」の可能性もあるので**断定しない**(「あなたのファイルです」と言い切らない)。検出は完璧でなくてよく、**最終的にユーザーの承認で確定**するのが安全弁。

設計書には**1行1ファイル**で、次の列を付けて一覧化する:

| 列 | 中身 |
|---|---|
| ファイル | パス |
| 区分 | ②新規 / ③手編集 |
| これは何か | 役割の1行 |
| 衝突 | ②で新に同名があるか(あれば③扱いで差分) |
| 配線 | `knowledge/*` `templates/rules-*` は `builder.md` の配線が要る → コピーだけでは**眠る**。要/不要を明記 |
| おすすめ | 残す(自作版)/ v3版を採用 / マージ |

新バージョンが公式に同等機能を入れている場合は「おすすめ」にそれを書く。判断が重い行は「**要確認**」と印を付け、承認時に詳しく開く候補にする。

**③で「ただ古い版」と「ユーザー編集」を見分ける(おすすめ列の根拠)**: ③(中身が違う)には2種類ある。`diff 旧 新` の向きで判定する:
- **旧にしか無い行が 0 = ユーザー未編集の「ただ古い版」** → おすすめは **v3版を採用(運ばない)**。新版のほうが新しいので持ち込む必要がない。
- **旧に固有の行がある = ユーザーが追記/改変している** → おすすめは **マージ候補**(その固有分だけ新版に足す)。

VERSION も併せて見る(旧 < 新なら基本は新版優先)。例: テストで bk の `builder.md` は「新に4行・旧に0行」= ただ古い版だったので、運ばず v3版を採用が正解だった。

---

## フェーズ2:承認後にまとめて実行(設計書の通りに動かす)

ステップ1-A の移行設計書が承認されたら、その通りに**まとめて**実行する。承認済みなので、ここでの新たな相談は「要確認」と印を付けた行だけ:

1. **軽いデータ(①)を AI が自動コピー** — `aboutme.md` / `ai-behavior.md` / `domain-knowledge.md` / `design-knowledge.md` / `revisions.md`+`revisions/` / `recipes/`。新フォルダ側の空テンプレは上書き可、**既に中身入りなら一言知らせてから**。**同一でもスキップせず実際にコピーし「✅ 運びました」と報告する**(パートA の❌参照。"差分なしだから不要"で省略しない)。
2. **`output/` はユーザー自身が運ぶ** — AI は**自動コピーしない**。**まず手動コピー(Finder(Mac)/ エクスプローラー(Windows)で `output` フォルダを新本体にドラッグ)を平易に案内**し、運べたら確認してから先へ。**コマンドが欲しいと言われた時だけ** rsync(除外つき)を渡す。旧環境前提のポート・絶対パスは運んだ後に各プロジェクトで個別調整(中身は書き換えない)。
3. **②の自作ファイルをコピー** — 承認された「残す」分だけ。
4. **③の手編集を当てる** — **自動マージしない**。旧と新の該当箇所を差分で見せ、承認された分だけ Edit で再適用(新版で構造が変わっていることがあるので丸ごと上書きしない)。
5. **「要確認」の行をここで詰める** — 設計書で保留した重い判断だけ、差分を開いて確定してから当てる。

> **②と③はセットで初めて動く**: `knowledge/*` `templates/rules-*` は `builder.md` の配線(③)を当てるまで、コピーされても**眠ったまま**。承認設計書の「配線」列が要だった行は、③再適用まで動かない。

### 完了報告

最後に3行でまとめる:

```
移行完了:
- 自動コピー(データ): {運んだ項目}
- 自作ファイル: {コピーした数} 個(うち配線の再適用が必要: {数} 個)
- 手編集: {再適用した数} / {検出した数} 個
```

②でコピーしたが③配線を当てていないファイルが残っている場合は、**「これらは配線を当てるまでまだ使われません」と明記**する(ユーザーが「ファイルはあるのに動かない」と混乱しないように)。

---

## 移行のあと(その先にやること)

移行コマンドが終わったら、新本体が正しく動くか**この順で確認**する。AI が案内する:

1. **起動確認**: 「ビルダーを起動」と言って、設計書プレビューが普通に出るか。`aboutme.md` / `ai-behavior.md` の内容が反映されているか(名前・スタックの好みが効いているか)。
2. **生成プロジェクトの確認**(`output/` を運んだ場合): 各プロジェクトには旧環境のポート番号・絶対パスが残っていることがある。気になるプロジェクトを開いて「これ動く?」と聞き、AI に個別調整させる(移行は中身を書き換えず運ぶだけなので、ここは生成プロジェクト側で直す)。
3. **自作カスタマイズの再適用確認**(フェーズ2で②③があった場合): ②でコピーした `knowledge/*` や `templates/rules-*` は、③の `builder.md` 配線を当てて初めて動く。配線を当てたら、その用途で**1つ試しに生成**して、自作ノウハウが効いているか確かめる。
4. **玄関(ダッシュボード)の再構築**: 「ダッシュボード見せて」と言うと、運んだプロジェクトが新本体の `dashboard.html`(生成プロジェクト一覧)に載る。移行直後は初期データのままなので、この一手で玄関が現状に追いつく。
5. **古いフォルダの片付け**: 上記で問題なければ、古いフォルダはアーカイブ/削除して OK。移行は非破壊なので、**しばらく残して安心してから**消すのを勧める(急がない)。

## 手動でやりたい場合(AI に任せず自分で運ぶ)

AI のコマンドを使わず手で移したい時の手順。**新本体フォルダの中で**作業する(`OLD` を旧フォルダの絶対パスに置き換える)。

**A. データを運ぶ(= フェーズ1。あるものだけ)**

```bash
OLD=/path/to/old-builder   # ← 旧フォルダの絶対パス
cp -f  "$OLD"/aboutme.md "$OLD"/ai-behavior.md ./ 2>/dev/null
cp -f  "$OLD"/domain-knowledge.md "$OLD"/design-knowledge.md ./ 2>/dev/null
# output は再生成可能フォルダを除外して運ぶ(.git は残す)。実測例で 4.3GB → 約116MB
rsync -a \
  --exclude=node_modules --exclude=.venv --exclude=venv --exclude=.next \
  --exclude=dist --exclude=build --exclude=.astro --exclude=__pycache__ \
  "$OLD"/output/ ./output/ 2>/dev/null
cp -f  "$OLD"/revisions.md ./ 2>/dev/null ; cp -rf "$OLD"/revisions ./ 2>/dev/null
cp -rf "$OLD"/recipes ./ 2>/dev/null
```

新本体側は配布初期値(空テンプレ)なので上書きで問題ない。新本体側に既に書き込んでいた場合だけ、上書き前に退避する。⏭️ スキップした再生成フォルダは、移行後に各プロジェクトで `pnpm install`(または README 記載のコマンド)を1回流せば復元する。

**B. 自分で足した新規ファイルを運ぶ(= フェーズ2の②)**

旧フォルダにあって新本体に**無い**ファイル(自分が追加した `knowledge/*.md` や `templates/rules-*.md` など)を手でコピーする。新本体に**同名がある**ものは中身が違う可能性があるので、コピーせず次の C で差分を見る。

**C. 手で入れた配線・追記を当て直す(= フェーズ2の③)**

`builder.md` などに自分で書き足した配線・追記は、**新本体の同ファイルに手で当て直す**。旧と新を差分で見比べ、自分の変更分だけを移す(新版で構造が変わっている場合があるので、丸ごと上書きはしない)。

> **重要**: B でコピーしたファイルは、C の配線を当てるまで**置かれるだけで動かない**(`knowledge/*` `templates/rules-*` は `builder.md` の配線があって初めて読まれる)。B と C はセットで初めて機能する。

終わったら、上の「移行のあと」1〜3で動作確認する。

---

## 配布物に含めない

このファイル自体は**配布してよい**(移行手順は受け取った人が使う)。一方、移行で運ぶ `revisions.md` / `revisions/` / `CONCEPT.md` は非配布物。`scripts/build-dist.sh` の forbidden ガードに従う。
